<nav class="bg-white border-b border-gray-200 px-4 py-3 flex items-center justify-between max-w-screen-xl mx-auto">
  <div class="text-2xl font-bold text-blue-600">
    <a href="/">Simple Social</a>
  </div>
  <div class="space-x-6 text-gray-700 font-medium flex items-center">
    <%= link_to 'Home', "/", class: "hover:text-blue-600 transition" %>
    <%= link_to 'Profile', user_signed_in? ? user_profile_path(current_user) : new_user_session_path, class: "hover:text-blue-600 transition" %>
    <%= link_to 'Users', users_path, class: "hover:text-blue-600 transition" %>
    <%= link_to "Create Post", user_signed_in? ? new_user_post_path(current_user) : new_user_session_path, class: "bg-blue-600 hover:bg-blue-700 text-white font-semibold py-2 px-4 rounded-md shadow transition" %>
    
    <div class="space-x-2 text-gray-700 font-medium flex items-center">
      <% if user_signed_in? %>
        <%= link_to "Logout", destroy_user_session_path, data: { turbo_method: :delete }, class: "text-white border border-red-600 bg-red-600 hover:bg-red-700 font-semibold py-1.5 px-4 rounded-md transition" %>
      <% else %>
        <%= link_to 'Login', new_user_session_path, class: "border border-blue-600 text-blue-600 hover:bg-blue-600 hover:text-white font-semibold py-1.5 px-4 rounded-md transition" %>
        <%= link_to 'Sign Up', new_user_registration_path, class: "border border-blue-600 text-blue-600 hover:bg-blue-600 hover:text-white font-semibold py-1.5 px-4 rounded-md transition" %>
      <% end %>
    </div>
  </div>
</nav>
